language: go
sudo: true

services:
  - docker

install:
  - sudo apt-get install build-essential
  - make docker
  - make run

script:
  - make func-test
  - make fill-test
  - make perform-test

after_script:
  - make stop

